# Tasks Document

## 当前状态 (2026-01-12)

**ESLint 检查结果**: ❌ 未通过
- **总问题数**: 5,513 (5,070 错误, 443 警告)
- **可自动修复**: 1,703 (1,567 错误, 136 警告)
- **需手动修复**: 3,810 (约 69%)

**📚 相关文档**:
- **状态报告**: `apps/desktop/CODEBASE_QUALITY_STATUS.md` - 执行摘要和行动计划
- **详细报告**: `apps/desktop/ESLINT_VIOLATIONS_REPORT.md` - 完整的违规分析
- **修复指南**: `apps/desktop/QUICK_FIX_GUIDE.md` - 常见问题的快速修复方法
- **进度跟踪**: `apps/desktop/scripts/check-lint-progress.sh` - 检查修复进度

### 主要问题分类
1. **架构违规** (P0): flows 层依赖 utils 层等
2. **函数式违规** (P1): console.log, Date 构造函数, try-catch, 对象变异
3. **类型安全** (P2): 缺少 readonly 修饰符, 可变数据
4. **代码风格** (P3): 箭头函数体, 文件命名

### 修复优先级
- **P0 (阻塞性)**: 架构违规 - 必须立即修复
- **P1 (高优先级)**: 函数式违规 - 影响代码质量
- **P2 (中优先级)**: 类型安全和文件命名
- **P3 (低优先级)**: 代码风格优化

### 快速开始
```bash
# 查看当前状态
bash apps/desktop/scripts/check-lint-progress.sh

# 自动修复可修复的问题
cd apps/desktop && npm run lint:grain -- --fix

# 再次检查进度
bash apps/desktop/scripts/check-lint-progress.sh
```

---

## Task Overview

本文档详细列出了代码库质量改进的所有具体任务,按优先级和执行顺序组织。

## Phase 1: ESLint 规则完善 (已完成 ✅)

### Task 1.1: 修复 no-mutation 规则 ✅
- **状态**: 已完成
- **描述**: 修复消息模板格式问题，重新启用规则
- **文件**: `apps/desktop/eslint-plugin-grain/src/rules/no-mutation.ts`
- **验证**: 所有测试通过 (27/27)

### Task 1.2: 添加 prefer-pipe 规则
- **状态**: 待实现
- **描述**: 检测嵌套函数调用，建议使用 fp-ts pipe
- **优先级**: P2
- **预估时间**: 4 小时

```typescript
// 检测模式
func3(func2(func1(data))) // ❌
pipe(data, func1, func2, func3) // ✅
```

### Task 1.3: 添加 no-promise-catch 规则
- **状态**: 待实现
- **描述**: 检测 Promise.catch()，建议使用 TaskEither
- **优先级**: P2
- **预估时间**: 4 小时

```typescript
// 检测模式
promise.catch(handleError) // ❌
pipe(taskEither, TE.orElse(handleError)) // ✅
```

### Task 1.4: 完善测试覆盖
- **状态**: 待实现
- **描述**: 为新规则添加完整测试用例
- **优先级**: P2
- **预估时间**: 2 小时

## Phase 2: 架构违规修复 (高优先级) ✅

### Task 2.1: 修复 io/log/logger.api.ts 架构违规 ✅
- **状态**: 已完成
- **描述**: io 层不应依赖 flows 层
- **优先级**: P0 (阻塞性)
- **解决方案**: 
  1. 创建了 `pipes/log/log-creation.pipe.ts` 纯函数
  2. 创建了 `io/log/simple-logger.api.ts` 符合架构的简化版本
  3. 重构了 `logger.api.ts` 使用依赖注入模式
  4. 验证通过：无架构违规错误

### Task 2.2: 修复其他层级依赖违规 ⚠️
- **状态**: 发现大量违规
- **描述**: 系统性检查发现 5,347 个 ESLint 违规
- **优先级**: P0
- **当前发现的主要问题**:
  - 文件命名违规：flows 目录文件缺少 .flow.ts 后缀
  - 架构违规：flows 层依赖 utils 层
  - 函数式违规：大量 console.log、try-catch、throw 语句
  - 类型安全：缺少 readonly 修饰符
  - 预估时间**: 20-30 小时（需要分阶段处理）

## Phase 3: 函数式编程违规修复

### Task 3.1: 批量替换 console.log 使用 🔥
- **状态**: 待执行
- **描述**: 将所有 console.* 调用替换为 logger API
- **优先级**: P1
- **预估时间**: 4 小时
- **影响文件**: ~200+ 个文件

**自动替换脚本**:
```bash
#!/bin/bash
# 批量替换 console 调用
find apps/desktop/src -name "*.ts" -type f -exec sed -i 's/console\.log(/logger.info(/g' {} \;
find apps/desktop/src -name "*.ts" -type f -exec sed -i 's/console\.error(/logger.error(/g' {} \;
find apps/desktop/src -name "*.ts" -type f -exec sed -i 's/console\.warn(/logger.warn(/g' {} \;
find apps/desktop/src -name "*.ts" -type f -exec sed -i 's/console\.debug(/logger.debug(/g' {} \;

# 添加必要的 import
find apps/desktop/src -name "*.ts" -type f -exec grep -l "logger\." {} \; | \
  xargs grep -L "from.*logger" | \
  xargs sed -i '1i import { logger } from "@/io/log/logger.api";'
```

**验证步骤**:
1. 运行替换脚本
2. 检查编译错误
3. 手动审查关键文件
4. 运行测试套件

### Task 3.2: 替换 Date 构造函数使用
- **状态**: 待执行
- **描述**: 将 Date 构造函数替换为 dayjs
- **优先级**: P1
- **预估时间**: 3 小时
- **影响文件**: ~50+ 个文件

**替换规则**:
```typescript
// 自动替换模式
new Date() → dayjs()
new Date(string) → dayjs(string)
Date.now() → dayjs().valueOf()
date.getTime() → dayjs(date).valueOf()
```

### Task 3.3: 转换 try-catch 为 TaskEither
- **状态**: 待执行
- **描述**: 将异步错误处理转换为函数式模式
- **优先级**: P1
- **预估时间**: 8 小时
- **影响文件**: ~30+ 个文件

**转换模板**:
```typescript
// 转换前
try {
  const result = await riskyOperation();
  return result;
} catch (error) {
  console.error(error);
  return null;
}

// 转换后
import * as TE from "fp-ts/TaskEither";
import { pipe } from "fp-ts/function";

const safeOperation = (): TE.TaskEither<AppError, Result> =>
  pipe(
    TE.tryCatch(
      () => riskyOperation(),
      (error) => createAppError('OPERATION_FAILED', String(error))
    )
  );
```

### Task 3.4: 消除 lodash 使用
- **状态**: 待执行
- **描述**: 将 lodash 函数替换为 es-toolkit
- **优先级**: P1
- **预估时间**: 2 小时

**替换映射**:
```typescript
import _ from 'lodash' → import * as toolkit from 'es-toolkit'
_.debounce → toolkit.debounce
_.cloneDeep → toolkit.cloneDeep
_.isEqual → toolkit.isEqual
```

## Phase 4: 文件命名规范化

### Task 4.1: pipes/ 目录文件重命名
- **状态**: 待执行
- **描述**: 将 pipes 目录文件添加 .pipe.ts 后缀
- **优先级**: P2
- **预估时间**: 2 小时
- **影响文件**: ~20+ 个文件

**重命名脚本**:
```bash
#!/bin/bash
cd apps/desktop
find src/pipes -name "*.ts" -not -name "*.pipe.ts" -not -name "index.ts" | while read file; do
  newname="${file%.ts}.pipe.ts"
  echo "Renaming: $file → $newname"
  git mv "$file" "$newname"
done
```

### Task 4.2: flows/ 目录文件重命名
- **状态**: 待执行
- **描述**: 将 flows 目录文件添加 .flow.ts 后缀
- **优先级**: P2
- **预估时间**: 2 小时
- **影响文件**: ~30+ 个文件

### Task 4.3: io/api/ 目录文件重命名
- **状态**: 待执行
- **描述**: 将 API 文件添加 .api.ts 后缀
- **优先级**: P2
- **预估时间**: 1 小时
- **影响文件**: ~10+ 个文件

### Task 4.4: 其他目录文件重命名
- **状态**: 待执行
- **描述**: state/, utils/, types/ 等目录文件重命名
- **优先级**: P2
- **预估时间**: 3 小时

### Task 4.5: 更新 import 语句
- **状态**: 待执行
- **描述**: 批量更新所有 import 路径
- **优先级**: P2
- **预估时间**: 4 小时

**更新策略**:
1. 使用 TypeScript Language Server API
2. 获取所有文件引用关系
3. 批量更新 import 路径
4. 验证编译无错误

## Phase 5: 类型安全提升

### Task 5.1: 添加 readonly 修饰符
- **状态**: 待执行
- **描述**: 为接口属性添加 readonly 修饰符
- **优先级**: P3
- **预估时间**: 3 小时

**检测规则**:
```typescript
// 需要修复的模式
interface Config {
  apiUrl: string;     // ❌ 缺少 readonly
  timeout: number;    // ❌ 缺少 readonly
}

// 修复后
interface Config {
  readonly apiUrl: string;     // ✅
  readonly timeout: number;    // ✅
}
```

### Task 5.2: 消除 any 类型使用
- **状态**: 待执行
- **描述**: 将 any 类型替换为具体类型或 unknown
- **优先级**: P3
- **预估时间**: 4 小时

**替换策略**:
```typescript
// 常见替换模式
any → unknown (需要类型守卫)
any[] → unknown[] 或具体类型数组
Record<string, any> → Record<string, unknown>
```

### Task 5.3: 完善类型定义
- **状态**: 待执行
- **描述**: 补充缺失的类型定义
- **优先级**: P3
- **预估时间**: 2 小时

## Phase 6: 代码结构优化

### Task 6.1: 简化箭头函数体
- **状态**: 待执行
- **描述**: 移除不必要的大括号和 return 语句
- **优先级**: P3
- **预估时间**: 2 小时

**简化规则**:
```typescript
// 单表达式函数体
const fn = (x) => { return x * 2; }  // ❌
const fn = (x) => x * 2;             // ✅
```

### Task 6.2: 函数拆分和重构
- **状态**: 待执行
- **描述**: 拆分过长函数，降低复杂度
- **优先级**: P3
- **预估时间**: 6 小时

**拆分原则**:
- 函数长度 > 50 行 → 拆分
- 圈复杂度 > 10 → 简化
- 嵌套层级 > 4 → 提取函数

## Phase 7: 持续质量保证

### Task 7.1: 配置 Git Hooks
- **状态**: 待实现
- **描述**: 设置 pre-commit 和 pre-push hooks
- **优先级**: P2
- **预估时间**: 1 小时

**Hook 配置**:
```bash
# .git/hooks/pre-commit
#!/bin/sh
npm run lint:grain
if [ $? -ne 0 ]; then
  echo "ESLint 检查失败，请修复后再提交"
  exit 1
fi
```

### Task 7.2: CI/CD 集成
- **状态**: 待实现
- **描述**: 在 GitHub Actions 中集成代码质量检查
- **优先级**: P2
- **预估时间**: 2 小时

### Task 7.3: 质量报告生成
- **状态**: 待实现
- **描述**: 生成修复前后的对比报告
- **优先级**: P3
- **预估时间**: 3 小时

## 执行计划

### Week 1: 基础设施和架构修复
- **Day 1-2**: Phase 1 (ESLint 规则完善)
- **Day 3-5**: Phase 2 (架构违规修复)

### Week 2: 函数式编程改进
- **Day 1-2**: Task 3.1 (console.log 替换)
- **Day 3**: Task 3.2 (Date 构造函数替换)
- **Day 4-5**: Task 3.3 (try-catch 转换)

### Week 3: 文件规范化和质量提升
- **Day 1-2**: Phase 4 (文件命名规范化)
- **Day 3-4**: Phase 5 (类型安全提升)
- **Day 5**: Phase 6 (代码结构优化)

### Week 4: 质量保证和文档
- **Day 1**: Phase 7 (持续质量保证)
- **Day 2-3**: 全面测试和验证
- **Day 4-5**: 文档更新和培训材料

## 验证清单

### 每个 Phase 完成后验证
- [ ] ESLint 检查通过 (`npm run lint:grain`) - **当前状态: 5,513 问题 (5,070 错误, 443 警告)**
- [ ] 所有测试通过 (`npm test`)
- [ ] 构建成功 (`npm run build`)
- [ ] 类型检查通过 (`npm run type-check`)

### 最终验证
- [ ] ESLint 错误数 = 0
- [ ] 架构违规数 = 0
- [ ] 函数式违规数 = 0
- [ ] 文件命名违规数 = 0
- [ ] 代码覆盖率 > 80%
- [ ] 构建时间无显著增加
- [ ] 功能测试全部通过

## 风险缓解

### 高风险任务
1. **Task 2.1**: 架构重构可能影响现有功能
   - 缓解: 渐进式重构，保持向后兼容
   - 回滚: 独立分支，便于回滚

2. **Task 3.1**: 大量文件修改可能引入错误
   - 缓解: 自动化脚本 + 人工审查
   - 验证: 完整测试套件

3. **Phase 4**: 文件重命名可能导致合并冲突
   - 缓解: 分批进行，及时同步
   - 协调: 团队沟通，避免并行修改

## 成功标准

### 技术指标
- ESLint 违规数从 500+ 降至 0
- 代码覆盖率提升至 80%+
- 构建时间保持在合理范围内

### 团队指标
- 开发者满意度 > 4.0/5.0
- 代码审查效率提升 30%
- 新人上手时间减少 50%

### 长期指标
- Bug 率降低 40%
- 功能开发速度提升 25%
- 代码维护成本降低 35%